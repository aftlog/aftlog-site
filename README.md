# AftLog — Site

Landing page + content funnel for AftLog (keeps this separate from the app repo by design — see DECISIONS.md #7).

Landing draft lives at `~/aftlog/aftlog-landing.html` until deployed here.
